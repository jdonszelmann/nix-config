builds:
  include:
    - '*.x86_64-linux.*'
    - '*.aarch64-linux.*'
    - '*.x86_64-darwin.*'
    - '*.aarch64-darwin.*'
    - 'nixosConfigurations.*'
    # These don't get picked up but it's okay; we re-export under `checks`.
    - 'darwinConfigurations.*'
    - 'homeConfigurations.*'